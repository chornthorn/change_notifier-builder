name: change_notifier_builder
description: Change notifier builder for changeNotifier
version: 1.0.0
homepage: https://github.com/chornthorn/change_notifier_builder

environment:
  sdk: '>=2.13.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# The following section is specific to Flutter packages.
flutter:

